
// operators, work on data
/**
 * mathematical, logical
 *  unary, binary, ternary
 *
 *      binary
 *          +       sum
 *          -       minus
 *          /       divide
 *          *       multiply
 *          %       modulus
 *          =       assignment
 *          ==      comparison
 *          ===     strict comparison
 *          >       GT
 *          >=      GTE
 *          <       LT
 *          <=      LTE
 *
 *      unary
 *                  x = x+1 or x = x-1
 *          x++     post increment
 *          ++x     pre increment
 *          x--     post decrement
 *          --x     pre decrement
 *
 *      ternary
 *                  x = x < 1 ? x : 1;
 *          ? :     ternary operator
 */
 //  (times === 0) ? a+b : (a+b)*times;