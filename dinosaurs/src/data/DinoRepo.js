// importing a module from node_modules or node core modules use just the name
import * as fs from 'fs';

let db = [];

/*

*/
/*
export function writeArray(){
    fs.appendFile('../../db/dino.json', JSON.stringify(db), (err) => {
        if(err)
    });
}
*/